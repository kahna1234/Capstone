meta {
  name: Validate Token
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/validateToken
  body: json
  auth: inherit
}

body:json {
  {
    "token": ""
  }
}

vars:pre-request {
  baseUrl: http://localhost:8088
}

example {
  name: 200 Response
  description: OK
  
  request: {
    url: {{baseUrl}}/auth/validateToken
    method: POST
    mode: json
    body:json: {
      {
        "token": ""
      }
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
